#include "main.h"
 #include "N_Custom/declars/Aton/Loader.hpp"

 namespace Loader { //In Loader namespace
   void RotateTo(double pos, int vel, bool wait){ //Rotate to Spesicic position
    //  LoadMotor.moveAbsolute(pos,vel);
    //  if(wait) while(LoadMotor.getActualVelocity()>0) pros::delay(5); //Wait untill it gets there if prefered
   }
 } /* Loader */
